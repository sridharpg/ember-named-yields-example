<ul>
  {{yield to='form-flare'}}
  <FormGroup @group={{this.args.orderForm}}></FormGroup>
  {{#each @groups as |group index|}}
    <FormGroup @group={{group}} @index={{index}}>
      {{yield group index to='group-header'}}
    </FormGroup>
  {{/each}}
</ul>